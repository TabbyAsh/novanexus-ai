# Phase 1 Evidence (Integrity Contract)

Date: 2026-02-08  
Environment: Local dev (services not running)  
Route: http://localhost:3030/internal/verify

## Status
PARTIAL (code-level verification completed; runtime verification pending)

## Runtime Attempt
```
curl.exe http://localhost:3030/internal/verify
curl: (7) Failed to connect to localhost port 3030 after 2238 ms: Could not connect to server
```

## Invariant Evidence (code paths)
1) **Provider outage → fallback returns non-null candles with provenance/confidence**  
   Marketdata `/v1/market/candles` now bypasses stale/missing cache entries and hot-swaps when the primary provider is unavailable. The response includes integrity tags even when synthetic/fallback is used.  
   - `services/marketdata/src/index.ts` (cache bypass + integrity propagation)

2) **Degraded confidence → live blocked, paper allowed**  
   Execution gating evaluates integrity `confidence_score`, `latency_class`, and `source_type`; low confidence demotes to paper, very low confidence blocks.  
   - `services/tradebot/src/index.ts` (`evaluateExecutionGate`, `/api/nexus/execute`)

3) **Missing integrity fields anywhere → hard reject**  
   Scan/thesis/paper trading/nexus endpoints now return 422 with `INTEGRITY_MISSING` and never silently succeed when integrity is absent.  
   - `services/tradebot/src/index.ts` (scanner integrity checks, `respondIntegrityFailure`, paper trade pricing)

4) **Provider recovery → hot-swap restores higher confidence + live eligibility**  
   Marketdata cache bypass prefers primary provider when healthy and integrity indicates higher confidence/latency class; execution gate lifts live eligibility once thresholds are met.  
   - `services/marketdata/src/index.ts` (provider priority + cache bypass)  
   - `services/tradebot/src/index.ts` (`evaluateExecutionGate`)

## Runtime Verification Steps (pending)
1. Start services: `npm run nova`
2. Enable internal verify in `nova-hub` env:
   - `INTERNAL_VERIFY_ENABLED=true`
   - `INTERNAL_VERIFY_TOKEN=...`
3. Run:
```
curl -H "x-internal-verify-token: $INTERNAL_VERIFY_TOKEN" \
  "http://localhost:3030/internal/verify"
```
