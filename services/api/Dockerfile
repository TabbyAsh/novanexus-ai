FROM python:3.11-slim

WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements
COPY pyproject.toml .

# Install Python dependencies
RUN pip install --no-cache-dir -e .

# Copy service code
COPY . .

# Copy core module
COPY ../core /app/services/core

# Set Python path
ENV PYTHONPATH=/app:/app/services/core:/app/services/bots

# Create data directory
RUN mkdir -p /app/data

# Expose port
EXPOSE 8000

# Run the application
CMD ["python", "-m", "uvicorn", "nova_api.main:app", "--host", "0.0.0.0", "--port", "8000"]
